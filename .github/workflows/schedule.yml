name: Schedule
on:
  workflow_dispatch: {}    # 手動実行を許可
  schedule:                # 定期実行イベント
    - cron: '2-59/5 * * * *'  # 2分から59分までの範囲を、5分おきに実行する(scheduleイベントの最小単位は5分)
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: date